# AI 学英语 App - 运行与打包依赖
# 安装:  pip install -r requirements.txt

# GUI 运行依赖 (Tkinter 为 Python 标准库, 通常已自带)
# 语音识别 / 大模型 (均为可选, 无 Key 时自动降级)
openai>=1.0.0
pyaudio>=0.2.11
Pillow>=10.0.0
requests>=2.28.0

# 打包
pyinstaller>=6.0.0
